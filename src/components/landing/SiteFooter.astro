---
import { getLangFromUrl, useTranslations, useRoutes } from '../../i18n/ui';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const r = useRoutes(lang);

const year = new Date().getFullYear();

const copy =
	lang === 'it'
		? {
				address: 'Contrada Montincello 20A, 71019, Vieste (FG)',
				vat: 'P. Iva 01831630684 · C.I.N. IT071060B400020484 · Licenza APFG000056-0070',
			}
		: {
				address: 'Contrada Montincello 20A, 71019, Vieste (FG), Italy',
				vat: 'VAT 01831630684 · C.I.N. IT071060B400020484 · License APFG000056-0070',
			};

const navItems = [
	{ key: 'home', label: t('nav.home') },
	{ key: 'case-vacanza', label: t('nav.apartments') },
	{ key: 'listino-prezzi', label: t('nav.prices') },
	{ key: 'regolamento', label: t('nav.rules') },
	{ key: 'galleria-immagini', label: t('nav.gallery') },
	{ key: 'appartamento-in-centro-paese', label: t('nav.town') },
] as const;
---

<footer class="site-footer">
	<div class="container">
		<nav class="footer-nav">
			{navItems.map((item) => <a href={r(item.key)}>{item.label}</a>)}
		</nav>

		<p class="contact-line">
			{copy.address} · <a href="tel:+393475097773">+39 347 509 7773</a> ·
			<a href="mailto:tenumont@gmail.com">tenumont@gmail.com</a>
		</p>
		<p class="vat-line">{copy.vat}</p>

		<div class="footer-widgets">
			<a href="https://www.lovelyitalia.it/" target="_blank" rel="nofollow noopener">
				<img
					src="https://www.lovelyitalia.com/grafica/banner/siamosu234x60white.png"
					width="234"
					height="60"
					loading="lazy"
					alt="Lastminute e offerte di Hotel, Bed and Breakfast, Agriturismi in tutta Italia"
				/>
			</a>
			<a href="https://www.bed-and-breakfast.it" target="_blank" rel="nofollow noopener">
				<img
					src="https://www.bed-and-breakfast.it/img/widget/siamo_su_200x59.png"
					width="200"
					height="59"
					loading="lazy"
					alt="Siamo su Bed-and-Breakfast.it"
				/>
			</a>
			<a href="https://www.ultimissimominuto.com/" target="_blank" rel="nofollow noopener">
				<img
					src="https://www.ultimissimominuto.com/grafica/banner/ul180x30.gif"
					width="180"
					height="30"
					loading="lazy"
					alt="UltimissimoMinuto"
				/>
			</a>
		</div>

		<div class="legal-row">
			<p class="copyright">© {year} Tenuta Montincello</p>
			<div class="links">
				<a href={r('privacy-policy')}>{t('footer.privacy')}</a>
				<a href={r('cookie-policy')}>{t('footer.cookie')}</a>
			</div>
		</div>
	</div>
</footer>

<style>
	.site-footer {
		padding: 4em 0 3em;
		background: var(--color-panel);
		font-size: 0.9em;
	}

	.footer-nav {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5em 1.4em;
		margin-bottom: 1.6em;
	}

	.footer-nav a {
		color: var(--color-fg);
	}

	.contact-line,
	.vat-line {
		color: var(--color-fg-light);
		margin-bottom: 0.4em;
	}

	.footer-widgets {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 1.4em;
		margin-top: 1.2em;
	}

	.footer-widgets a {
		display: inline-block;
		opacity: 0.85;
		transition: opacity 0.2s ease;
	}

	.footer-widgets a:hover {
		opacity: 1;
	}

	.footer-widgets img {
		display: block;
		height: auto;
		max-width: 100%;
	}

	.legal-row {
		display: flex;
		flex-wrap: wrap;
		justify-content: space-between;
		align-items: center;
		gap: 1em;
		margin-top: 2em;
		padding-top: 1.6em;
		border-top: 1px solid var(--color-border-bg);
	}

	.copyright {
		margin: 0;
		color: var(--color-fg-light);
	}

	.links {
		display: flex;
		gap: 1.2em;
	}

	.links a {
		color: var(--color-fg-light);
	}
</style>
